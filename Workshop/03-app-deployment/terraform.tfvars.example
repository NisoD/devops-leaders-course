# Example configuration file
# Copy this to terraform.tfvars and customize as needed

kubeconfig_path = "../02-terraform-k8s/kubeconfig"
app_name       = "sample-app"
namespace      = "devops-app"
replica_count  = 3
app_port       = 5000
service_port   = 80
